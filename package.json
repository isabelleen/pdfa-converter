{
  "name": "pdfa-converter",
  "version": "0.1.0",
  "description": "Free, client-side PDF to PDF/A converter (runs entirely in the browser via Ghostscript-WASM).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "node browser-test.mjs",
    "deploy": "vite build && wrangler pages deploy dist --project-name=pdfa-converter --commit-dirty=true"
  },
  "dependencies": {
    "@jspawn/ghostscript-wasm": "^0.0.2"
  },
  "devDependencies": {
    "puppeteer-core": "^25.3.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.9",
    "wrangler": "^4.106.0"
  }
}
